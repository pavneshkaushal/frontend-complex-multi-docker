import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('reders without crashing', ()=>{});
it('reders without crashing', ()=>{});
it('reders without crashing', ()=>{});
it('reders without crashing', ()=>{});
it('reders without crashing', ()=>{});